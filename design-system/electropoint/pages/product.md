# Product detail overrides

- Gallery left (sticky on desktop), buy box right
- Image stage: muted surface, object-contain, no drop-shadow circus
- Specs: grouped accordion, label muted / value foreground, tabular numbers
- Variant swatches: color = circular 24px with name on focus; storage = segmented control
- Price block: current large, was strikethrough, discount as text `-12%`
- Mobile: gallery then buy box; sticky bar with price + add to cart
